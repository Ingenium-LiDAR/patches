# patches
Patch repository for ingenium computers. Whenever you release a new patch, name it "#.sh", where # is the next integer higher than the previous highest patch number. When Ingenium computers boot (though for now, not the G16), they will search this repository and download and run th
